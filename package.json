{
  "name": "partysummoner",
  "version": "1.0.0",
  "description": "Party Summoner front end",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steve Mason",
  "license": "ISC",
  "devDependencies": {
    "bulma": "^0.9.0",
    "node-sass": "^4.14.1"
  },
  "scripts": {
    "css-build": "node-sass --omit-source-map-url library/style.scss style.css",
    "css-watch": "npm run css-build -- --watch",
    "start": "npm run css-watch"
  }
}

